# Project-Scoped Rules

- **Git Commit and Push Policy**: Do not commit, push, or suggest updating code on GitHub after small changes or local builds. All workspace developments must remain strictly local until the user explicitly requests or triggers a deployment/push command.
